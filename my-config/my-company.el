;;; package --- configure for company
;;; code:
;;; Commentary:

(custom-set-variables
 '(global-company-mode t)
)
(setq company-auto-complete t)
(setq company-auto-complete-chars nil)
(setq company-idle-delay 0.2)
(setq company-minimum-prefix-length 3)


